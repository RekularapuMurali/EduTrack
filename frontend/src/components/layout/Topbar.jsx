export default function Topbar({ title }) {
  return <div style={{ height: 60, background: '#fff', borderBottom: '1px solid #E2E8F0', padding: '0 24px', display: 'flex', alignItems: 'center' }}>{title}</div>;
}